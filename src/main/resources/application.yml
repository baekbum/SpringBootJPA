spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/jpashop;
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        #show_sql: true
        format_sql: true
        default_batch_fetch_size: 500 # in query를 사용할 때
    #open-in-view: false

  #thymeleaf:
  #  prefix: classpath:/templates/
  #  suffix: .html

#logging:
#  level:
#    org.hibernate.SQL: debug
#    org.hibernate.type: trace